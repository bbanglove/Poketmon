<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="designprjalgorithm.goketmon">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Option1Activity" />
        <activity android:name=".Option2Activity" />
        <activity android:name=".OutputActivity" />
        <activity android:name=".Option3Activity" />
        <activity android:name=".Output2Activity" />
        <activity android:name=".Output3Activity"></activity>
    </application>

</manifest>